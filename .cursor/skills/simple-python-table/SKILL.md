---
name: simple-python-table
description: Minimal Python demo that exposes get_user_table() returning a pandas DataFrame. Use when working with the simple-python-table sample in this repo or illustrating small table/DataFrame examples.
---

# Simple Python Table

## Run

```bash
pip install -r requirements.txt
python main.py
```

`get_user_table()` in `main.py` returns a simple table as a `pandas.DataFrame`.
